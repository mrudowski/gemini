import elm from './assets/images/elm.png';
import hazel from './assets/images/hazel.png';

import {IActorId} from '../../engine/Dialogue/types';

const portraits: Record<IActorId, string> = {
  elm,
  hazel,
};

export default portraits;
